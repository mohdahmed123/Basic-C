#include <stdio.h>
void swap(int a,int b)
{
    int c;
    c=a;
    a=b;
    b=c;
    
    printf("after the swapping\n");
    printf("a is %d",a);
    printf("b is %d",b);
    
}
void main()
{   int a,b;
    printf("enter your first and second mumber=");
    scanf("%d%d",&a,&b);
    swap(a,b);
}
